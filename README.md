# text-processing-engine-c
A C-based text processing engine that parses raw multi-line input, performs normalization (case folding and punctuation trimming), filters stop-words and numeric tokens, and computes word frequency statistics using manual data structures without associative containers.
